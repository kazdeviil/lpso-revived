using UnityEngine;

public class NPCSpeechBubble : MonoBehaviour
{
    // TODO:
    // Disable two of X / Arrow / Menu GameObjects when one of them is visible
    // Disable button hitbox GameObject when Menu is visible
    // Change the color of the "speaker" text, relative to who is "speaking" (purple for an NPC, orange for a minigame...)
    // Change the size of the Menu to account for several options
    // Change the size of the speech bubble when the dialogue spans more than one line
}
